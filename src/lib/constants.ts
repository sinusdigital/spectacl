/**
 * Layout constants — JS mirror of CSS custom properties defined in globals.css.
 *
 * The canonical value lives in CSS (--header-h). This constant exists only for
 * JS calculations that need a number, e.g. sticky stacking offsets.
 * If you change --header-h, update this too.
 */

/** Matches --header-h in globals.css */
export const PAGE_HEADER_HEIGHT = 56;
